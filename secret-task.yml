apiVersion : v1
kind : Secret
metadata:
  name : nodejs-web-secret 
type: Opaque      # Opaque => custom secrets(most comon), k8s.io/tls =>  certicates, k8s.io/basic-auth => username & password
data:
  username : YWRtaW4=      # base64 format (use echo -n "admin" | base64)
  password : bm9kZWpz      # base64 format (use echo -n "nodejs" | base64)